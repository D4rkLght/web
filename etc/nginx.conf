
events {
    worker_connections  1024;
}

server {
	listen 80;
	server_name localhost;

	location ^~ /uploads/ {
	    root /home/box/web/;
	}

	location ^~ /img/ {
	    root /home/box/web/public/;
	}
}		
