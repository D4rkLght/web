
upstream backend {
    server 0.0.0.0:8080;
}

server {
	listen 80;
	server_name localhost;

	location / {
		proxy-pass http://backend;
	}

	location ^~ /uploads/ {
	    root /home/box/web/;
	}

	location *~ (/./w*)$ {
	    root /home/box/web/public/;
	}
}		
